
import Workspace from '@/components/Workspace.vue';

const routes = [
    {
        path: '/workspace',
        component: Workspace,
        name: 'Workspace',
        hidden: true,
        // children: [
        // ],
    },

    // TODO add more...
];

export default routes;
